{
    /* Это пример комментария */
    var pi = 3.14
    settings -> {
        name -> "example",
        values -> [1, 2, 3],
        nested -> {
            enabled -> true,
            timeout -> 30
        }
    }
}
